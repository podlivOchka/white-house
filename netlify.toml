[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[build.environment]
  NODE_VERSION = "24"
  VITE_BASE_PATH = "/"
  VITE_PREVIEW = "true"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
