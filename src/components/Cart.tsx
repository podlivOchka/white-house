import {ArrowUpRight,MessageCircle,Minus,Plus,ShoppingBag,X} from "lucide-react";
import {getProduct,money,whatsappUrl} from "../data/catalog";
import {lineKey,orderMessage,total,type CartLine} from "../lib/cart";
export default function Cart({cart,onRemove,onQuantity,onCatalog}:{cart:CartLine[];onRemove:(key:string)=>void;onQuantity:(key:string,delta:number)=>void;onCatalog:()=>void}){
const summary=total(cart);
if(!cart.length)return <div className="empty-state cart-empty"><ShoppingBag size={40} strokeWidth={1}/><h3>Здесь будет ваш образ</h3><p>Выберите вещь в каталоге и добавьте её в корзину.</p><button className="dark-button" onClick={onCatalog}>Открыть каталог<ArrowUpRight size={17}/></button></div>;
return <><div className="cart-list">{cart.map(line=>{const p=getProduct(line.id)!;const key=lineKey(line);return <div className="cart-line" key={key}><img src={p.image} alt={p.name} width="90" height="140"/><div><span className="product-brand">WHITE HOUSE</span><h3>{p.name}</h3><p>{line.size} · {line.color}</p><strong>{p.price===null?"Цена по запросу":money(p.price*line.quantity)}</strong><div className="quantity"><button onClick={()=>onQuantity(key,-1)} disabled={line.quantity===1} aria-label={"Уменьшить количество: "+p.name}><Minus size={14}/></button><span>{line.quantity}</span><button onClick={()=>onQuantity(key,1)} disabled={line.quantity===99} aria-label={"Увеличить количество: "+p.name}><Plus size={14}/></button></div></div><button className="remove-line" onClick={()=>onRemove(key)} aria-label={"Удалить из корзины: "+p.name}><X size={16}/></button></div>;})}</div><div className="cart-total"><div><span>{summary.unknown?"Известная стоимость":"По ценам каталога"}</span><strong data-testid="cart-total">{money(summary.amount)}</strong></div>{summary.unknown&&<p>Стоимость моделей без цены уточнит консультант.</p>}<a className="dark-button full-width" href={whatsappUrl(orderMessage(cart))} target="_blank" rel="noreferrer">Перейти в WhatsApp<MessageCircle size={18}/></a><p>Откроется готовое сообщение. Отправьте его консультанту, чтобы согласовать заказ. Корзина сохраняется на этом устройстве.</p></div></>;
}
